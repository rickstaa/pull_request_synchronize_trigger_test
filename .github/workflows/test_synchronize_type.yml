name: Theme preview

on:
  pull_request:
    types: [opened, edited, reopened, synchronize]
    branches:
      - master
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Triggered
        run: echo "Triggered"
      - name: Context
        run: echo "$GITHUB_CONTEXT"
